# セキュリティガイド

## 実装済みセキュリティ機能

### 🔐 認証トークン暗号化

**概要**: ローカルストレージに保存される認証トークンを暗号化してXSS攻撃から保護

**実装内容**:
- **暗号化方式**: XOR暗号化 + Base64エンコーディング
- **暗号化キー**: ブラウザフィンガープリント基盤（デバイス固有）
- **識別子**: `enc:` プレフィックスで暗号化データを識別
- **自動処理**: ログイン時の自動暗号化、読み込み時の自動復号化

**セキュリティ効果**:
- ✅ XSS攻撃でローカルストレージが盗まれても解読困難
- ✅ ブラウザ固有の暗号化キーで他デバイスでは復号不可
- ✅ 軽量な暗号化でパフォーマンス影響なし
- ✅ 後方互換性で既存データも正常処理

**確認方法**:
```javascript
// ブラウザコンソールで実行
localStorage.getItem('auth_data')
// 結果: "enc:ShDQVl1cMNYIEgQNS3FXDG8j..." (暗号化済みの場合)
```

### 🔔 プッシュ通知システム

**概要**: Firebase Cloud Messaging (FCM) によるセキュアな通知システム

**実装内容**:
- **FCMトークン管理**: 暗号化された認証データを使用
- **フォアグラウンド通知**: カスタム通知システム
- **ローカル保存**: 通知履歴をローカルストレージに保存
- **バッジ更新**: 未読通知数のリアルタイム更新

**セキュリティ機能**:
- ✅ 認証済みユーザーのみがFCMトークンを登録可能
- ✅ 暗号化された認証トークンを使用してサーバーと通信
- ✅ 通知権限の適切な管理とエラーハンドリング

### 🛡️ Laravel Sanctum 認証

**概要**: APIトークンベースの認証システム

**実装内容**:
- **トークン管理**: 8時間有効（Remember Me: 7日間）
- **自動ログアウト**: トークン期限切れ時の自動処理
- **セッション管理**: トークン期限チェッカー機能
- **エラーハンドリング**: 401エラーの適切な処理

## セキュリティベストプラクティス

### 開発時の注意事項

1. **トークンの取り扱い**
   - ログ出力時にトークンを含めない
   - デバッグ情報からトークンを除外
   - コンソールログでの機密情報出力を避ける

2. **暗号化の確認**
   ```javascript
   // 新しいログイン後は必ず暗号化されていることを確認
   const authData = localStorage.getItem('auth_data');
   if (!authData.startsWith('enc:')) {
       console.warn('Token is not encrypted!');
   }
   ```

3. **環境変数の管理**
   - Firebase設定は環境変数で管理
   - APIキーやシークレットをハードコーディングしない
   - `.env`ファイルをバージョン管理から除外

### セキュリティチェックリスト

- [ ] 新規ログイン時にトークンが暗号化されている (`enc:` プレフィックス)
- [ ] FCMトークンが正常に送信されている
- [ ] 認証エラー時の適切なログアウト処理
- [ ] 通知権限の適切な管理
- [ ] ローカルストレージの適切なクリア処理

## 脆弱性報告

セキュリティ上の問題を発見した場合は、以下の手順で報告してください：

1. **緊急度の高い脆弱性**: 直接開発者に連絡
2. **一般的な問題**: GitHubのIssueで報告
3. **改善提案**: Pull Requestで提案

## 今後の予定

### 計画中のセキュリティ強化

- 📋 **CSRF保護**: クロスサイトリクエストフォージェリ対策
- 📋 **レート制限**: API呼び出し頻度制限
- 📋 **入力検証強化**: SQLインジェクション対策
- 📋 **HTTPS強制**: 本番環境での暗号化通信
- 📋 **セキュリティヘッダー**: XSS、CSRF、Clickjacking対策

### セキュリティ監査

- 定期的なセキュリティテスト実施
- 脆弱性スキャナーによる自動チェック
- コードレビュー時のセキュリティ確認

---

**最終更新**: 2025年9月9日
**適用バージョン**: v1.2.0以降